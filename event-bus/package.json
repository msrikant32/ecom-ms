{
  "name": "ecom-event-bus",
  "version": "1.0.0",
  "description": "Durable message queue engine (SQLite-backed): producers publish an event, it fans out to one or more named queues, consumers pull/ack with automatic redelivery and dead-lettering. Local stand-in for SQS/SNS.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node --test",
    "demo": "node scripts/watch-demo.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18"
  }
}
